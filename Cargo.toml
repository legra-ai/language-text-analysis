[package]
name = "language-text-analysis"
version = "0.1.3"
edition = "2024"
authors = ["Jacobus Geluk <jacobus.geluk@dataroad.com>"]
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Small, deterministic multilingual text-analysis pipeline for search and document processing"
repository = "https://github.com/legra-ai/language-text-analysis"
homepage = "https://github.com/legra-ai/language-text-analysis"
keywords = ["text", "search", "tokenization", "stemming", "unicode"]
categories = ["text-processing", "algorithms"]
readme = "README.md"

[dependencies]
rust-stemmers = "1"
serde = { version = "1", features = ["derive"] }
unicode-normalization = "0.1"
unicode-segmentation = "1"

[build-dependencies]
rhusky = "0.0.6"

[lints.rust]
missing_docs = "deny"

[lints.clippy]
all = "warn"
pedantic = "warn"
